import styled from "styled-components";

const VideoMainContainer = styled.section`
display:flex;
justify-content: center;
margin: 3rem 0;
`
const VideoContainer = styled.iframe`
height: 500px;
width: 900px;
`

export {VideoMainContainer,VideoContainer};